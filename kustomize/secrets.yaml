apiVersion: v1
kind: Secret
metadata:
  name: goty-tls-secret
  namespace: mi-goty-namespace
type: kubernetes.io/tls
data:
  goty-tls-key: ${${GOTY_KEY | b64enc}}
  goty-tls-crt: ${${GOTY_CRT | b64enc}}