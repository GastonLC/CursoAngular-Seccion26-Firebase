resources:
  - GOTY-namespace.yaml
  - GOTY-deployment.yaml
  - GOTY-ingress.yaml

secretGenerator:
  - name: goty-tls-secret
    files:
      - goty-tls-key
      - goty-tls-crt
      
generators:
  - secrets.yaml